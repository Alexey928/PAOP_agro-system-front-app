export type Worker = {
    id:string
    name:string,
    surname:string,
    age:number,
    birthday:Date,
}
type workerHash = {[key:string]:Worker}


export const workersReduser  = (a:string) => {
    switch (a) {

    }
}

